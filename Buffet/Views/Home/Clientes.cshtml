@using Buffet.ViewModels.Home
@{
    ViewData["Title"] = "Clientes";
}
<h1>@ViewData["Title"]</h1>

<p>Listagem de clientes.</p>

<ul>
    @foreach (Cliente cliente in Model.Clientes)
    {
        <li>@cliente.Nome nascido em @cliente.Nome</li>
    }
</ul>